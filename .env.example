# ── Required ─────────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=CHANGEME

# ── Remote model API (agentplatform.ru or any OpenAI-compatible endpoint) ────
REMOTE_API_BASE_URL=https://api.agentplatform.ru/v1
REMOTE_API_KEY=sk-ap-CHANGEME

# ── Open WebUI secret (generate: python -c "import secrets; print(secrets.token_hex(32))") ──
WEBUI_SECRET_KEY=CHANGEME_32_CHAR_HEX_STRING
