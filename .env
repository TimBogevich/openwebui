# Functional secrets only (DB + remote Qwen API). No auth/admin — login is disabled.
POSTGRES_PASSWORD=Gcu2026!
REMOTE_API_BASE_URL=https://api.agentplatform.ru/v1
# REMOTE_API_KEY intentionally NOT committed — set it locally before `docker compose up`.
REMOTE_API_KEY=
